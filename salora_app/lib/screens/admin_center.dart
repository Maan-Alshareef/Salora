import 'package:flutter/material.dart';

class AdminCenter extends StatelessWidget {
  const AdminCenter({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('لوحة الإدارة متوفرة عبر داشبورد الويب.')));
}
